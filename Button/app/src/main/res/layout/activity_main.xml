<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/raised_btn"
        android:text="@string/raised_button"
        android:layout_marginTop="24dp"
        style="@style/RaisedButton"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/flat_btn"
        android:text="@string/flat_button"
        android:layout_marginTop="24dp"
        style="@style/FlatButton"
        app:layout_constraintTop_toBottomOf="@id/raised_btn"
        app:layout_constraintStart_toStartOf="@id/raised_btn"
        app:layout_constraintEnd_toEndOf="@id/raised_btn"/>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/fab"
        android:layout_margin ="16dp"
        android:elevation="8dp"
        android:src="@drawable/ic_baseline_mail_24"
        app:fabSize="normal"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:contentDescription="@string/email_content" />


</androidx.constraintlayout.widget.ConstraintLayout>